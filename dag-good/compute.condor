universe = vanilla

output = ./logs/compute.$(cluster).$(process).out 
error = ./logs/compute.$(cluster).$(process).err 
log = ./logs/compute.$(cluster).$(process).log

requirements = (OpSys == "LINUX" && Arch == "X86_64")

executable = compute.sh
transfer_input_files = foobar.py
should_transfer_files = YES
when_to_transfer_output = ON_EXIT
# transfer_output_files = baz.txt

arguments = "$(MYARGS)"
queue